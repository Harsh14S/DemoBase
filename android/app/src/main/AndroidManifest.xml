<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/blackpanther"
      android:roundIcon="@mipmap/blackpanther_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme">
      <!-- <activity android:name=".MainActivity" /> -->
      <!-- <activity android:name=".MainActivity" android:exported="true"/> -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <!-- <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter> -->
      </activity>

      <!-- antman -->
      <activity-alias
        android:name=".MainActivityantman"
        android:label="@string/app_name"
        android:enabled="true"
        android:exported="true"
        android:icon="@mipmap/antman"
        android:roundIcon="@mipmap/antman_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- blackpanther -->
      <activity-alias
        android:name=".MainActivityblackpanther"
        android:label="@string/app_name"
        android:enabled="false"
        android:exported="true"
        android:icon="@mipmap/blackpanther"
        android:roundIcon="@mipmap/blackpanther_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- deadpool -->
      <activity-alias
        android:name=".MainActivitydeadpool"
        android:label="@string/app_name"
        android:enabled="false"
        android:exported="true"
        android:icon="@mipmap/deadpool"
        android:roundIcon="@mipmap/deadpool_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- groot -->
      <activity-alias
        android:name=".MainActivitygroot"
        android:label="@string/app_name"
        android:enabled="false"
        android:exported="true"
        android:icon="@mipmap/groot"
        android:roundIcon="@mipmap/groot_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- ironman -->
      <activity-alias
        android:name=".MainActivityironman"
        android:label="@string/app_name"
        android:enabled="false"
        android:exported="true"
        android:icon="@mipmap/ironman"
        android:roundIcon="@mipmap/ironman_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- spiderman -->
      <activity-alias
        android:name=".MainActivityspiderman"
        android:label="@string/app_name"
        android:enabled="false"
        android:exported="true"
        android:icon="@mipmap/spiderman"
        android:roundIcon="@mipmap/spiderman_round"
        android:targetActivity=".MainActivity">
        <intent-filter>
          <action android:name="android.intent.action.MAIN" />
          <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>
    </application>
</manifest>
